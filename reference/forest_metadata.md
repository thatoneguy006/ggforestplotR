# Inspect forest-data metadata

Returns the semantic and provenance metadata attached to a `forest_data`
object. Plotting behavior is determined by this metadata rather than by
the class of the original fitted model.

## Usage

``` r
forest_metadata(x)
```

## Arguments

- x:

  A `forest_data` object.

## Value

A named metadata list.
