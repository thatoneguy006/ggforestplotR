## Test environments

- Windows 10 x64, R 4.5.2

## R CMD check results

0 errors | 1 warning | 4 notes

## Notes

- `qpdf` is not installed in the current environment, which triggers the standard PDF size-reduction warning during `--as-cran` checks.
- CRAN incoming URL checks could not be fully verified because this environment does not have outbound network access.
- `unable to verify current time` is an environment note from the local check machine.
- The package is still using development metadata (`Version: 0.0.0.9000` and placeholder maintainer details), so it is not yet ready for a real CRAN submission.
