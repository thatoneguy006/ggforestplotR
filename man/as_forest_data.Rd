% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_forest_data.R, R/tidy_forest_model.R
\name{as_forest_data}
\alias{as_forest_data}
\alias{as_forest_data.forest_data}
\alias{as_forest_data.data.frame}
\alias{as_forest_data.lm}
\alias{as_forest_data.glm}
\alias{as_forest_data.coxph}
\alias{as_forest_data.merMod}
\alias{as_forest_data.lme}
\alias{as_forest_data.glmmTMB}
\alias{as_forest_data.default}
\title{Standardize coefficient data for forest plots}
\usage{
as_forest_data(data, ...)

\method{as_forest_data}{forest_data}(
  data,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending"),
  exponentiate = NULL,
  ...
)

\method{as_forest_data}{data.frame}(
  data,
  term,
  estimate,
  conf.low,
  conf.high,
  label = term,
  term_labels = NULL,
  group = NULL,
  grouping = NULL,
  separate_groups = NULL,
  n = NULL,
  events = NULL,
  p.value = NULL,
  exponentiate = NULL,
  estimate_scale = NULL,
  axis_transform = NULL,
  effect_label = NULL,
  conf.level = 0.95,
  reference_value = NULL,
  source_model = NULL,
  source_package = NULL,
  sort_terms = c("none", "descending", "ascending"),
  subgroup = NULL,
  ...
)

\method{as_forest_data}{lm}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)

\method{as_forest_data}{glm}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)

\method{as_forest_data}{coxph}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)

\method{as_forest_data}{merMod}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)

\method{as_forest_data}{lme}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)

\method{as_forest_data}{glmmTMB}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)

\method{as_forest_data}{default}(
  data,
  ...,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = NULL,
  intercept = FALSE,
  term_labels = NULL,
  sort_terms = c("none", "descending", "ascending")
)
}
\arguments{
\item{data}{A data frame or data-frame subclass containing coefficient
estimates and intervals. Tibbles and \code{data.table} objects are supported.}

\item{...}{Arguments passed to an \code{as_forest_data()} method.}

\item{term_labels}{Optional named vector used to relabel displayed terms.
Names should match values in the term column and values are the labels to
display.}

\item{sort_terms}{How to sort rows: \code{"none"}, \code{"descending"}, or
\code{"ascending"}. Subgroup hierarchies require \code{"none"} so their source order
is preserved.}

\item{exponentiate}{Compatibility argument. \code{TRUE} is equivalent to
\code{estimate_scale = "ratio"}; \code{FALSE} is equivalent to
\code{estimate_scale = "identity"} when \code{estimate_scale} is not supplied.}

\item{term}{Column name holding the model term identifier.}

\item{estimate}{Column name holding the point estimate.}

\item{conf.low}{Column name holding the lower confidence bound.}

\item{conf.high}{Column name holding the upper confidence bound.}

\item{label}{Optional column name used for the displayed row label.}

\item{group}{Optional column name used for color-grouping multiple
estimates per row.}

\item{grouping}{Optional column name used to split rows into grouped plot
sections.}

\item{separate_groups}{Optional column name used to identify labeled
variable blocks that can be outlined with separator lines.}

\item{n}{Optional column name holding sample sizes or other N labels for
table helpers.}

\item{events}{Optional column name holding event counts or event labels for
table helpers.}

\item{p.value}{Optional column name holding p-values.}

\item{estimate_scale}{Semantic scale of the stored estimates. One of
\code{"identity"}, \code{"log"}, \code{"ratio"}, \code{"probability"}, or
\code{"risk_difference"}.}

\item{axis_transform}{Transformation used for the plotting axis. Defaults
to \code{"log10"} for ratios and \code{"identity"} otherwise.}

\item{effect_label}{Short label for the effect measure, such as \code{"Beta"},
\code{"OR"}, \code{"HR"}, \code{"RR"}, or \code{"RD"}.}

\item{conf.level}{Confidence level represented by the interval columns, or
\code{NA} when it is unknown.}

\item{reference_value}{Numeric null/reference value, or \code{NULL} when the
effect measure has no universal reference value.}

\item{source_model}{Optional character vector identifying the source model
class. The complete fitted model is not retained.}

\item{source_package}{Optional package name identifying the model source.}

\item{subgroup}{Optional column name defining hierarchical subgroup blocks.
Missing or empty values identify ordinary standalone estimates. Rows with
the same non-empty value must form one contiguous block within each facet.
Subgroups are never inferred and do not calculate model contrasts.}

\item{conf.int}{Logical; model methods require \code{TRUE} because forest data
include confidence-interval columns.}

\item{intercept}{Logical; for model methods, whether to retain the
intercept term.}
}
\value{
A \code{forest_data} data-frame subclass ready for \code{\link[=ggforestplot]{ggforestplot()}} and
the table composition helpers. Original data-frame columns are retained
for table helpers so they can be displayed with
\code{add_forest_table(columns = ...)}.
}
\description{
Standardizes a coefficient table into the internal forest-plot data
structure used throughout \code{ggforestplotR}.
}
\examples{
raw <- data.frame(
  variable = c("Age", "BMI", "Treatment"),
  beta = c(0.10, -0.08, 0.34),
  lower = c(0.02, -0.16, 0.12),
  upper = c(0.18, 0.00, 0.56)
)

as_forest_data(
  data = raw,
  term = "variable",
  estimate = "beta",
  conf.low = "lower",
  conf.high = "upper"
)
}
