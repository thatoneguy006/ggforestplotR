% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{prefix_ambiguous_labels}
\alias{prefix_ambiguous_labels}
\title{Within each panel, if a separate_groups value appears more than once the
labels are ambiguous.  Prefix them with "group: label" so the axis is
readable.}
\usage{
prefix_ambiguous_labels(data, has_groupings)
}
\description{
Within each panel, if a separate_groups value appears more than once the
labels are ambiguous.  Prefix them with "group: label" so the axis is
readable.
}
