% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_favors.R
\name{add_favors}
\alias{add_favors}
\title{Add directional favors labels beneath a forest plot}
\usage{
add_favors(
  plot = NULL,
  left,
  right,
  reference = NULL,
  gap = 0.02,
  footer_height = 0.4,
  text_size = 3.2,
  linewidth = 0.5,
  arrow_length = 0.08,
  arrow_type = c("closed", "open")
)
}
\arguments{
\item{plot}{A plot created by \code{\link[=ggforestplot]{ggforestplot()}} or a supported forest-table
composition. Leave as \code{NULL} to use \code{+ add_favors(...)} syntax.}

\item{left, right}{Single strings shown beneath the left and right arrows.}

\item{reference}{Optional numeric reference value. \code{NULL} uses the reference
line resolved by \code{\link[=ggforestplot]{ggforestplot()}}, falling back to the null value stored in
the forest-data metadata.}

\item{gap}{Gap on each side of the trained reference position, expressed as
a fraction of the forest panel width.}

\item{footer_height}{Footer height in inches.}

\item{text_size}{Text size passed to \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}.}

\item{linewidth}{Line width passed to \code{\link[ggplot2:geom_segment]{ggplot2::geom_segment()}}.}

\item{arrow_length}{Arrowhead length in inches.}

\item{arrow_type}{Whether arrowheads are \code{"closed"} or \code{"open"}.}
}
\value{
A patchwork-composed plot with a footer beneath only the forest-plot
column, or a ggplot add-on object when \code{plot = NULL}.
}
\description{
Compose a two-sided arrow annotation beneath the trained forest-plot x
panel. The annotation is a separate footer plot, so it does not alter the
forest plot's scales, limits, confidence intervals, or reference line.
}
\details{
\code{add_favors()} is designed as the final composition step. It works with a
bare \code{\link[=ggforestplot]{ggforestplot()}} result and with layouts returned by
\code{\link[=add_forest_table]{add_forest_table()}} and \code{\link[=add_split_table]{add_split_table()}}.
}
\examples{
coefs <- data.frame(
  term = c("Age", "BMI", "Treatment"),
  estimate = c(0.10, -0.08, 0.34),
  conf.low = c(0.02, -0.16, 0.12),
  conf.high = c(0.18, 0.00, 0.56)
)

ggforestplot(coefs) +
  add_favors(
    left = "Treatment A better",
    right = "Treatment B better"
  )

add_favors(
  ggforestplot(coefs),
  left = "Treatment A better",
  right = "Treatment B better"
)
}
